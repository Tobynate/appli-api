Done: Full CRUD via Swagger, with DTO boundary cleanly drawn.
Tomorrow (Day 4): Extract IApplicationService + ApplicationService. Register in DI (AddSingleton). Controller depends on the interface, not the data. This is the day interfaces and DI become live working code.

